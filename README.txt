
Getting started
===============
gem install bundler
bundle install

# "Less CSS", our CSS DSL, is a node package. This will install a binary "lessc". Make sure the node
# binaries are on your PATH -- add ~/node_modules/.bin to your PATH if it's not there already.
npm install less
npm install coffeescript